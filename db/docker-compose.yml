services:
  postgres:
    image: postgres:16
    container_name: abm_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: abm
      POSTGRES_USER: abm
      POSTGRES_PASSWORD: abm123
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data